query get_scopes {
  scopes {
    ...Scope
  }
}

query aget_scope($id: String!) {
  scope(key: $id) {
    ...Scope
  }
}

query search_scopes($search: String) {
  options: scopes(search: $search) {
    value
    label
  }
}
